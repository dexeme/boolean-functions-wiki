Equivalence Relations
=====================

Content extracted from ``data/Equivalence_Relations.txt``.
Original page:

:link:`https://boolean.wiki.uib.no/Equivalence_Relations <https://boolean.wiki.uib.no/Equivalence_Relations>`

Some known Equivalence Relations
--------------------------------

Two vectorial Boolean functions
`F,F':\mathbb {F} _{2}^{n}\rightarrow \mathbb {F} _{2}^{m}` are called:

* affine (linear) equivalent if there exist `A_{1},A_{2}` affine (linear)
  permutations of `\mathbb {F} _{2}^{m}` and `\mathbb {F} _{2}^{n}`,
  respectively, such that `F'=A_{1}\circ F\circ A_{2}`;
* extended affine equivalent (shortly EA-equivalent) if there exists
  `A:\mathbb{F}_2^n\rightarrow\mathbb{F}_2^m` affine such that `F'=F''+A`,
  with `F''` affine equivalent to `F`;
* Carlet-Charpin-Zinoviev equivalent (shortly CCZ-equivalent) if there exists
  an affine permutation `\mathcal{L}` of
  `\mathbb{F}_2^n\times\mathbb{F}_2^m` such that the image of the graph of
  `F` is the graph of `F'`, i.e. `\mathcal{L}(G_F)=G_{F'}` with
  `G_F=\{ (x,F(x)) : x\in\mathbb{F}_2^n\}` and
  `G_{F'}=\{ (x,F'(x)) : x\in\mathbb{F}_2^n\}`.

A `Magma implementation <../codes/CCZeq2.html>`_ is available.

Clearly, it is possible to extend such definitions also for maps
`F,F':\mathbb{F}_p^n\rightarrow\mathbb{F}_p^m`, for `p` a general prime
number.

Connections between different relations
.......................................

Such equivalence relations are connected to each other. Obviously affine
equivalence is a general case of linear equivalence, and they are both a
particular case of EA-equivalence. Moreover, EA-equivalence is a particular
case of CCZ-equivalence and each permutation is CCZ-equivalent to its inverse.

In particular, CCZ-equivalence coincides with:

* EA-equivalence for planar functions;
* linear equivalence for DO planar functions;
* EA-equivalence for all Boolean functions;
* EA-equivalence for all bent vectorial Boolean functions;
* EA-equivalence for two quadratic APN functions.

Invariants
----------

The algebraic degree (if the function is not affine) is invariant under
EA-equivalence, but in general is not preserved under CCZ-equivalence.

The differential uniformity is invariant under CCZ-equivalence.
CCZ-equivalence is the most general known equivalence relation preserving APN
and PN properties.

Invariants in even characteristic
.................................

We consider now functions over `\mathbb{F}_2^n`. The nonlinearity and the
extended Walsh spectrum are invariant under CCZ-equivalence. The Walsh
spectrum is invariant under affine equivalence, but in general not under EA-
and CCZ-equivalence.

For APN maps we have also that `Delta-rank <../codes/DeltaRank_code.html>`_
and `Gamma-rank <../codes/GammaRank_code.html>`_ are invariant under
CCZ-equivalence.

To define such ranks, let us consider `F` an `(n,n)`-function and associate a
group algebra element `G_F` in `\mathbb{F}_2^n\times\mathbb{F}_2^n`:

`G_F=\sum_{v\in\mathbb{F}_2^n}(v,F(v))`.

For `F` APN there exists some subset
`D_F \in\mathbb{F}_2^n\times\mathbb{F}_2^n \setminus\{(0,0)\}` such that

`G_F\cdot G_F =2^n\cdot(0,0)+2\cdot D_F`.

The Gamma-rank is the dimension of the ideal generated by `G_F` in
`\mathbb{F}_2`, and the Delta-rank is the dimension of the ideal generated by
`D_F` in `\mathbb{F}_2`. Equivalently:

* the Gamma-rank is the rank of the incidence matrix of `dev(G_F)` over
  `\mathbb{F}_2`;
* the Delta-rank is the `\mathbb{F}_2`-rank of an incidence matrix of
  `dev(D_F)`.

For the incidence structure `dev(G_F)` with blocks

`G_F\cdot(a,b)=\{(x+a,F(x)+b) : x\in\mathbb{F}_2^n \}`,

for `a,b\in\mathbb{F}_2^n`, the related incidence matrix is constructed,
indexed by points and blocks, as follows: the `(p,B)`-entry is 1 if point `p`
is incident with block `B`, and is 0 otherwise. The same can be done for
`D_F`.

For APN maps, the `multiplier group <../codes/MGF_code.html>`_ `\mathcal{M}(G_F)`
is CCZ-invariant. The multiplier group `\mathcal{M}(G_F)` of `dev(G_F)` is
the set of automorphisms `\phi` of `\mathbb{F}_2^{2n}` such that
`\phi(G_F)=G_F\cdot(u,v)` for some `u,v\in\mathbb{F}_2^n`. Such automorphisms
form a group contained in the automorphism group of `dev(G_F)`.

References
==========

.. references::
